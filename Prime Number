#include<stdio.h>
main()
{
int i,a;
printf("Enter the number\n");
scanf(" %d",&a);
for(i=2;i<=a-1;i++)
{
	if(a%i==0)
	{
		printf("Not Prime\n");
	}
	
}
       	if(i==a)
	{
		printf("Number is prime");
	}
}
