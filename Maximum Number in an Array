#include<stdio.h>
main()
{
	int arr[10];
	int i,max;
	for(i=0;i<=9;i++)
	{
		printf("Enter a number\n");
		scanf(" %d",&arr[i]);
	}
		max=arr[0];
		for(i=0;i<=9;i++)
		{
			if(arr[i]>max)
			{
			max=arr[i];
			}
		}
		printf("Max is %d",max);
		
} 
